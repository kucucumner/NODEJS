console.log(Math.round(1.6)); // 2
console.log(Math.round(1.4)); // 1

function sum(first){ // 아규먼트를 받아서 함수 안으로 전달해주는 매개체를 parameter 라고 한다.
    return first + second; // return 은 어떤 값을 출력한다는 의미와 그 함수를 종료시키는 특수한 키워드

}

console.log(sum(2 , 4)); // 입력값 하나 하나를 argument 라고 한다. 